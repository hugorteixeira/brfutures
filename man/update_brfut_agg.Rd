% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{update_brfut_agg}
\alias{update_brfut_agg}
\title{Rebuild cached root and aggregate data}
\usage{
update_brfut_agg(root = NULL, all = TRUE, rebuild_roots = NULL, quiet = FALSE)
}
\arguments{
\item{root}{Optional character vector with roots to target. When omitted
and `rebuild_roots` is `TRUE`, every cached root is rebuilt from the raw
HTML files.}

\item{all}{When `TRUE`, refreshes the aggregate cache after the optional root
rebuilds. Defaults to `TRUE`.}

\item{rebuild_roots}{When `TRUE`, rebuilds root caches from the raw HTML
files for the selected roots. Defaults to `FALSE` for a fast aggregate-only
refresh.}

\item{quiet}{Set to `TRUE` to silence informational messages.}
}
\value{
Invisibly returns a list with rebuilt root data frames (if any) and
  the refreshed aggregate data frame when requested.
}
\description{
Rebuild cached root and aggregate data
}
