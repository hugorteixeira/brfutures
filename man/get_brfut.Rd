% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{get_brfut}
\alias{get_brfut}
\title{Retrieve cached B3 futures data}
\usage{
get_brfut(
  ticker,
  start = NULL,
  end = NULL,
  treatment = "ohlcv_drop0_xts",
  add_attrs = TRUE,
  rebuild_agg = FALSE,
  tz = "America/Sao_Paulo",
  keep_time = TRUE,
  ...
)
}
\arguments{
\item{ticker}{Character vector with specific contract tickers (e.g. `"WINZ24"`).}

\item{start, end}{Optional bounds restricting the returned dates.}

\item{treatment}{Either the name of a built-in treatment (e.g. `"raw"`,
`"standard"`, `"ohlcv_xts"`) or a function that receives the raw data frame
and returns the desired shape.}

\item{add_attrs}{When `TRUE` (default), attach futures metadata to the result.}

\item{rebuild_agg}{Set to `TRUE` to rebuild aggregates before retrieving
data. The relevant root caches are rebuilt from the raw HTML/XML files when
necessary.}

\item{tz}{Timezone used when returning `xts` objects. Defaults to
`"America/Sao_Paulo"`.}

\item{keep_time}{When `TRUE` (default), keep the clock time when assigning the
timezone (e.g. midnight stays midnight). When `FALSE`, shift timestamps to
the target timezone.}

\item{...}{Additional arguments forwarded to the treatment function.}
}
\value{
The result of applying `treatment` to the filtered bulletin rows.
}
\description{
Returns data combined from HTML and BVBG XML sources; the `source` column
indicates the origin (`html` or `xml`) before treatments are applied.
}
