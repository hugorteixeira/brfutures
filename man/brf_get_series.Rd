% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{brf_get_series}
\alias{brf_get_series}
\title{Retrieve a processed series for a specific contract ticker}
\usage{
brf_get_series(
  ticker,
  data_dir = NULL,
  which = c("cache", "data", "config"),
  type = c("full", "ohlcv_locf"),
  source = c("local", "sm_api"),
  verbose = FALSE,
  tz = "America/Sao_Paulo",
  is_DI = FALSE
)
}
\arguments{
\item{ticker}{Contract symbol (e.g. `"WDOZ24"`).}

\item{data_dir}{Directory containing saved bulletin files. Defaults to the
cache directory for the parent ticker root.}

\item{which}{Storage location passed to `tools::R_user_dir` when directories
are inferred.}

\item{type}{One of `"full"` or `"ohlcv_locf"` (forward-fill OHLC gaps per contract before
returning the data).}

\item{source}{Data source for the series: `"local"` reads cached bulletin
aggregates (guided by `which`) while `"sm_api"` calls `sm_get_data()` and
falls back to the local cache if the API is unavailable.}

\item{verbose}{If `TRUE`, emit informative messages.}

\item{tz}{Time zone assigned to the returned xts index.}

\item{is_DI}{If `TRUE`, treat the contract as a DI future and append `TickSize`
and `TickValue` columns derived from `calculate_futures_di_notional()`.}
}
\value{
An xts object containing the requested series; an empty data frame is
  returned when the ticker is not found. When `is_DI` is `TRUE`, DI futures
  outputs also include the `TickSize` and `TickValue` columns.
}
\description{
Retrieve a processed series for a specific contract ticker
}
