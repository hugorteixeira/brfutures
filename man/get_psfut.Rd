% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postgres.R
\name{get_psfut}
\alias{get_psfut}
\title{Retrieve futures data from PostgreSQL}
\usage{
get_psfut(
  ticker,
  table = "usa_futures_int_databento",
  ticker_col = "ticker",
  date_col = "date",
  root_col = "root",
  start = NULL,
  end = NULL,
  treatment = "ohlcv_drop0_xts",
  add_attrs = TRUE,
  tz = "America/Sao_Paulo",
  keep_time = TRUE,
  timeframe = "1d",
  host = getOption("brfutures.postgres.host", "localhost"),
  dbname = getOption("brfutures.postgres.dbname", NULL),
  user = getOption("brfutures.postgres.user", NULL),
  password = getOption("brfutures.postgres.password", NULL),
  port = getOption("brfutures.postgres.port", 5432),
  ...
)
}
\arguments{
\item{ticker}{Character vector with contract tickers (e.g. `"WINZ24"`).}

\item{table}{Character scalar with the base table name (optionally schema-qualified).}

\item{ticker_col}{Column name that stores the contract ticker.}

\item{date_col}{Column name that stores the observation date/time.}

\item{root_col}{Optional column name that stores the contract root (renamed to `root`).}

\item{start, end}{Optional bounds restricting the returned dates.}

\item{treatment}{Either the name of a built-in treatment (e.g. `"raw"`,
`"standard"`, `"ohlcv_xts"`) or a function that receives the raw data frame
and returns the desired shape.}

\item{add_attrs}{When `TRUE` (default), enrich output with futures metadata.}

\item{tz}{Timezone used when returning `xts` objects. Defaults to
`"America/Sao_Paulo"`.}

\item{keep_time}{When `TRUE` (default), keep the clock time when assigning the
timezone (e.g. midnight stays midnight). When `FALSE`, shift timestamps to
the target timezone.}

\item{timeframe}{Optional suffix used to locate a timeframe-specific table
(e.g. `"1d"` -> `table_1d`).}

\item{host, dbname, user, password, port}{PostgreSQL connection parameters.}

\item{...}{Additional arguments forwarded to the treatment function.}
}
\value{
The result of applying `treatment` to the filtered rows.
}
\description{
Queries a PostgreSQL table for specific tickers, applies the same treatments
as `get_brfut()`, and returns the filtered result.
}
\details{
Connection defaults can be provided via:
`options(brfutures.postgres.host = "localhost",
         brfutures.postgres.dbname = NULL,
         brfutures.postgres.user = NULL,
         brfutures.postgres.password = NULL,
         brfutures.postgres.port = 5432)`
}
