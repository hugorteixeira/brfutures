% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contract_registry.R
\name{brf_contract_resolve}
\alias{brf_contract_resolve}
\title{Resolve canonical B3 futures contract metadata}
\usage{
brf_contract_resolve(
  tickers,
  reference_date = NULL,
  registry = NULL,
  strict = TRUE,
  cal = NULL
)
}
\arguments{
\item{tickers}{Character vector of explicit dated B3 futures tickers.}

\item{reference_date}{Optional date used only to disambiguate two-digit years
for tickers absent from the official registry. Defaults to `Sys.Date()`.}

\item{registry}{Optional explicit registry, mainly for deterministic tests.}

\item{strict}{Whether unresolved rows should raise an error. With `FALSE`,
unresolved rows are returned with a diagnostic `status`.}

\item{cal}{Optional `bizdays` calendar used only by pre-2018 estimators.}
}
\value{
One canonical contract row per input ticker, in input order.
}
\description{
Resolution is exact and local. Tickers with maturity in 2018 or later must
exist in the official `BVBG.028` registry; absence is an error and never
falls back to a formula. Supported pre-2018 contracts use the historical B3
rules and the package's B3 trading-session calendar.
}
