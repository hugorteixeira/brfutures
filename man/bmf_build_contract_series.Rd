% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{bmf_build_contract_series}
\alias{bmf_build_contract_series}
\title{Build xts time series for a ticker root}
\usage{
bmf_build_contract_series(
  ticker_root,
  data_dir = NULL,
  out_dir = NULL,
  save_series = TRUE,
  add_agg = FALSE,
  which = c("cache", "data", "config"),
  verbose = TRUE,
  estimate_maturity = FALSE,
  return = c("list", "agg")
)
}
\arguments{
\item{ticker_root}{Commodity symbol root.}

\item{data_dir}{Directory containing saved bulletin files.}

\item{out_dir}{Directory where the xts objects will be saved (RDS files).}

\item{save_series}{If `TRUE`, persist each xts object as an `.rds` file.}

\item{add_agg}{If `TRUE`, also persist an aggregated data file containing all
contracts with a `ticker` column for downstream merges.}

\item{which}{Storage location passed to `tools::R_user_dir` when directories
are inferred.}

\item{verbose}{If `TRUE`, emit progress messages while building the series.}

\item{estimate_maturity}{If `TRUE`, estimate contract maturities using the
package calendar and store them in the output.}

\item{return}{Controls what is returned. `"list"` (default) returns the
per-contract xts objects; `"agg"` returns the aggregated data frame.}
}
\value{
A named list of xts objects; the list has an attribute `files` with
  the paths of the saved series (if `save_series = TRUE`).
}
\description{
Build xts time series for a ticker root
}
