% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/b3_price_reports.R
\name{brf_b3_prices_compare}
\alias{brf_b3_prices_compare}
\title{Compare complete and simplified B3 price reports}
\usage{
brf_b3_prices_compare(
  dates,
  root = NULL,
  cache_dir = NULL,
  refresh = FALSE,
  quiet = FALSE,
  tolerance = 1e-10
)
}
\arguments{
\item{dates}{Vector of B3 report dates.}

\item{root}{Optional futures root or vector of roots. Defaults to every
maturity-looking futures row in the reports.}

\item{cache_dir}{Optional B3 reference cache root.}

\item{refresh}{Force downloads even for valid completed cache entries.}

\item{quiet}{Suppress progress messages.}

\item{tolerance}{Relative numeric comparison tolerance.}
}
\value{
A list with daily coverage, aggregate field coverage, exact
  differences, unmatched rows and matched wide rows.
}
\description{
Fetches the final `BVBG.086` and `BVBG.187` snapshots for every requested
report date, joins futures by trade date, ticker and B3 instrument id, and
audits both row coverage and field equality. Full-only liquidity fields such
as `FinInstrmQty` and `NtlFinVol` are reported explicitly rather than being
confused with `RglrTxsQty` (the number of trades).
}
