% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{brf_get_aggregate}
\alias{brf_get_aggregate}
\title{Retrieve the aggregate data file for a ticker root}
\usage{
brf_get_aggregate(
  ticker_root,
  data_dir = NULL,
  which = c("cache", "data", "config"),
  type = c("ohlcv_locf", "full"),
  source = c("local", "sm_api"),
  return = c("agg", "list")
)
}
\arguments{
\item{ticker_root}{Commodity symbol root.}

\item{data_dir}{Directory containing saved bulletin files. Defaults to the
cache directory for the ticker when `NULL`.}

\item{which}{Storage location passed to `tools::R_user_dir` when directories
are inferred.}

\item{type}{One of `"full"` or `"ohlcv_locf"` (forward-fill OHLC gaps per contract before
returning the data).}

\item{source}{Data source for the aggregate: `"local"` reads cached bulletin
files (using `which` to resolve paths) while `"sm_api"` calls
`sm_get_data()` and falls back to the local cache if the API is
unavailable.}

\item{return}{Controls the output: `"agg"` (default) returns the aggregate
data frame; `"list"` returns a split list of data frames with a `path`
attribute referencing the aggregate file.}
}
\value{
A data frame with the aggregated data or an empty data frame if the
  aggregate is missing. Prints the path when found.
}
\description{
Retrieve the aggregate data file for a ticker root
}
