% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{bmf_get_series}
\alias{bmf_get_series}
\title{Retrieve a processed series for a specific contract ticker}
\usage{
bmf_get_series(
  ticker,
  data_dir = NULL,
  which = c("cache", "data", "config"),
  type = c("full", "ohlc", "ohlcv_locf"),
  verbose = FALSE,
  tz = "America/Sao_Paulo"
)
}
\arguments{
\item{ticker}{Contract symbol (e.g. `"WDOZ24"`).}

\item{data_dir}{Directory containing saved bulletin files. Defaults to the
cache directory for the parent ticker root.}

\item{which}{Storage location passed to `tools::R_user_dir` when directories
are inferred.}

\item{type}{One of `"full"`, `"ohlc"`, or `"ohlcv_locf"`.}

\item{verbose}{If `TRUE`, emit informative messages.}

\item{return}{Controls what is returned. `"agg"` (default) returns the
aggregate data frame; `"list"` returns a list containing the data and
underlying file path.}
}
\value{
A data frame containing the requested series; an empty data frame is
  returned when the ticker is not found.
}
\description{
Retrieve a processed series for a specific contract ticker
}
