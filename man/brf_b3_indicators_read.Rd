% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/b3_bit_sources.R
\name{brf_b3_indicators_read}
\alias{brf_b3_indicators_read}
\title{Read official B3 indicator observations used by BIT settlement}
\usage{
brf_b3_indicators_read(
  path,
  report_date = NULL,
  available_at,
  source_file = basename(path),
  indicators = c("BTCLIQUSD", "RTDOL-D1", "RTBITLIQ")
)
}
\arguments{
\item{path}{Path to the extracted `Indic.txt`.}

\item{report_date}{Date of the B3 daily indicator archive. It may be omitted
only when `source_file` contains a name such as `ID251223.ex_`.}

\item{available_at}{Causal publication/availability timestamp with a time
zone.}

\item{source_file}{Original archive or payload name retained as provenance.}

\item{indicators}{Indicator labels to retain.}
}
\value{
A data frame of fixed-width indicator observations and provenance.
}
\description{
Parses the fixed-width `Indic.txt` payload distributed in B3's daily
indicator archive. The defaults retain the Nasdaq Bitcoin liquidation
reference (`BTCLIQUSD`), the B3 D+1 BRL/USD rate (`RTDOL-D1`), and B3's
directly published BRL liquidation result (`RTBITLIQ`). `NQBTCS` and
`BTCLIQBRL` are accepted aliases but are never manufactured when absent.
}
\details{
`Indic.txt` does not embed an authoritative publication timestamp. Callers
must therefore provide `available_at` from the source-delivery evidence;
download time is not a historical substitute.
}
