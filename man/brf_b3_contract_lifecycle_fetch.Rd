% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/b3_reference_fetch.R
\name{brf_b3_contract_lifecycle_fetch}
\alias{brf_b3_contract_lifecycle_fetch}
\title{Fetch official B3 futures lifecycle source data}
\usage{
brf_b3_contract_lifecycle_fetch(
  date,
  cache_dir = NULL,
  refresh = FALSE,
  quiet = FALSE,
  latest = TRUE,
  root = "BIT"
)
}
\arguments{
\item{date}{B3 report date.}

\item{cache_dir}{Optional reference cache root. Defaults below
`options(brfutures.cache_dir)`.}

\item{refresh}{Whether to download the archive even when cached snapshots
exist.}

\item{quiet}{Suppress download progress messages.}

\item{latest}{Whether to return only the newest official revision per
contract and report date. Set to `FALSE` to retain corrections in causal
publication order.}

\item{root}{B3 futures root to retain. Defaults to `"BIT"` for backward
compatibility. Use `NULL` to retain every futures root in the snapshot.}
}
\value{
A causal futures lifecycle data frame for the report date.
}
\description{
Downloads `INyymmdd.zip` and retains the compressed archive in a
SHA-256-addressed cache. Nested `BVBG.028` candidates are ordered by their
official `AppHdr/CreDt`. They are extracted and parsed one at a time, then
immediately removed, so requesting all revisions does not retain multiple
hundreds-of-megabytes XML files on disk.
}
